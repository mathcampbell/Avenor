name: Apply terrain v14 river routing and probe fixes

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch terrain and biome probe
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          cpp_path = Path('AvenorClient/Source/AvenorEditor/AvenorStripTerrainGenerator.cpp')
          hdr_path = Path('AvenorClient/Source/AvenorEditor/AvenorStripTerrainGenerator.h')
          probe_path = Path('AvenorClient/Source/AvenorEditor/AvenorBiomeCameraProbe.cpp')
          cpp = cpp_path.read_text(encoding='utf-8')
          hdr = hdr_path.read_text(encoding='utf-8')
          probe = probe_path.read_text(encoding='utf-8')

          def one(text, old, new, name):
              n = text.count(old)
              if n != 1:
                  raise RuntimeError(f'{name}: expected one match, found {n}')
              return text.replace(old, new, 1)

          cpp = one(cpp,
              'static constexpr int32 GeneratorAlgorithmVersion = 13;',
              'static constexpr int32 GeneratorAlgorithmVersion = 14;',
              'algorithm version')

          cpp = one(cpp,
'''static double DrainageSourceMultiplier(
    const FAvenorStripData& Data,
    int32 Cell
)
{
    const double MountainSupport = Data.MountainMask.IsValidIndex(Cell)
        ? Data.MountainMask[Cell] : 0.0;
    const double HillSupport = Data.HillMask.IsValidIndex(Cell)
        ? Data.HillMask[Cell] : 0.0;
    const double ReliefSupport = FMath::Max(
        FMath::Max(MountainSupport, HillSupport * 0.75),
        Smooth01(FMath::Clamp(Data.Slope[Cell] / 0.055, 0.0, 1.0))
    );
    const double SourceMoisture = Data.MacroMoisture.IsValidIndex(Cell)
        ? Data.MacroMoisture[Cell]
        : (Data.Moisture.IsValidIndex(Cell) ? Data.Moisture[Cell] : 0.5);
    const double WetLowlandSupport = Smooth01(FMath::Clamp(
        (SourceMoisture - 0.62) / 0.26, 0.0, 1.0
    ));
    const double DesertInfluence = Data.DesertMask.IsValidIndex(Cell)
        ? Data.DesertMask[Cell] : 0.0;
    const double AridSourcePenalty = DesertInfluence * Smooth01(
        FMath::Clamp((0.52 - SourceMoisture) / 0.42, 0.0, 1.0)
    );
    const double SourceSupport = FMath::Max(
        ReliefSupport, WetLowlandSupport
    );
    return 1.0
        + (1.0 - SourceSupport) * 1.45
        + AridSourcePenalty * 1.65;
}
''',
'''static double DrainageSourceMultiplier(
    const FAvenorStripData& Data,
    int32 Cell
)
{
    const double MountainSupport = Data.MountainMask.IsValidIndex(Cell)
        ? Data.MountainMask[Cell] : 0.0;
    const double HillSupport = Data.HillMask.IsValidIndex(Cell)
        ? Data.HillMask[Cell] : 0.0;
    const double ReliefSupport = FMath::Max(
        FMath::Max(MountainSupport, HillSupport * 0.75),
        Smooth01(FMath::Clamp(Data.Slope[Cell] / 0.055, 0.0, 1.0))
    );
    const double SourceMoisture = Data.MacroMoisture.IsValidIndex(Cell)
        ? Data.MacroMoisture[Cell]
        : (Data.Moisture.IsValidIndex(Cell) ? Data.Moisture[Cell] : 0.5);
    const double WetLowlandSupport = Smooth01(FMath::Clamp(
        (SourceMoisture - 0.62) / 0.26, 0.0, 1.0
    ));
    const double DesertInfluence = Data.DesertMask.IsValidIndex(Cell)
        ? Data.DesertMask[Cell] : 0.0;
    const double AridSourcePenalty = DesertInfluence * Smooth01(
        FMath::Clamp((0.52 - SourceMoisture) / 0.42, 0.0, 1.0)
    );
    const double SourceSupport = FMath::Max(
        ReliefSupport, WetLowlandSupport
    );
    return 1.0
        + (1.0 - SourceSupport) * 1.45
        + AridSourcePenalty * 1.65;
}

// Geomorphic erosion remembers wetter climatic periods more readily than the
// final active-river network does. Dry regions can therefore retain valleys,
// canyons and dissected mesas even when present-day runoff is too small to
// render a permanent river through them.
static double ErosionDrainageSourceMultiplier(
    const FAvenorStripData& Data,
    int32 Cell
)
{
    const double ActiveMultiplier = DrainageSourceMultiplier(Data, Cell);
    const double Desert = Data.DesertMask.IsValidIndex(Cell)
        ? FMath::Clamp(Data.DesertMask[Cell], 0.0, 1.0) : 0.0;
    return FMath::Lerp(
        FMath::Min(ActiveMultiplier, 1.85),
        FMath::Min(ActiveMultiplier, 1.25),
        Desert
    );
}
''', 'erosion drainage multiplier')

          # Only the two erosion passes use palaeodrainage. The authoritative
          # active-river selector deliberately keeps DrainageSourceMultiplier.
          old = ') * DrainageSourceMultiplier(Data, Cell);'
          positions = []
          start = 0
          while True:
              i = cpp.find(old, start)
              if i < 0: break
              positions.append(i); start = i + 1
          if len(positions) < 4:
              raise RuntimeError(f'expected >=4 drainage multiplier uses, found {len(positions)}')
          # First two occurrences after helper are the stream-power and evolved-terrain passes.
          helper_end = cpp.index('static double ErosionDrainageSourceMultiplier')
          after = [i for i in positions if i > helper_end]
          if len(after) < 3:
              raise RuntimeError('could not isolate erosion multiplier uses')
          for target in reversed(after[:2]):
              cpp = cpp[:target] + ') * ErosionDrainageSourceMultiplier(Data, Cell);' + cpp[target+len(old):]

          cpp = one(cpp,
'''    const double MountainProvince = Smooth01(FMath::Clamp(
        (Province * 0.62 + Province2 * 0.38 - 0.56) / 0.30,
        0.0, 1.0
    ));
    const double HillProvince = Smooth01(FMath::Clamp(
        (Province * 0.42 + Province2 * 0.58 - 0.30) / 0.46,
        0.0, 1.0
    )) * (1.0 - MountainProvince * 0.42);''',
'''    const double MountainProvince = Smooth01(FMath::Clamp(
        (Province * 0.62 + Province2 * 0.38 - 0.59) / 0.29,
        0.0, 1.0
    ));
    const double HillProvince = Smooth01(FMath::Clamp(
        (Province * 0.42 + Province2 * 0.58 - 0.35) / 0.44,
        0.0, 1.0
    )) * (1.0 - MountainProvince * 0.46);''', 'province balance')

          cpp = one(cpp,
'''    const double BroadRange = Smooth01(FMath::Clamp(
        (BeltSignal - FMath::Lerp(0.44, 0.34, Activity)) / 0.38,
        0.0, 1.0
    )) * FMath::Lerp(0.48, 1.0, MountainProvince)
       * FMath::Lerp(0.70, 1.0, PositiveUplift);''',
'''    const double BroadRange = Smooth01(FMath::Clamp(
        (BeltSignal - FMath::Lerp(0.48, 0.38, Activity)) / 0.40,
        0.0, 1.0
    )) * FMath::Lerp(0.42, 1.0, MountainProvince)
       * FMath::Lerp(0.70, 1.0, PositiveUplift);''', 'mountain coverage')

          cpp = one(cpp,
'''    const double FoothillEnvelope = Smooth01(FMath::Clamp(
        (BeltSignal - 0.34) / 0.38, 0.0, 1.0
    )) * MountainProvince * (1.0 - OutMountainMask * 0.52);
    const double IndependentHills = HillProvince * Smooth01(FMath::Clamp(
        (0.40 * UplandRidges
            + 0.40 * (0.5 + 0.5 * Rolling)
            + 0.20 * Province2 - 0.33) / 0.46,
        0.0, 1.0
    ));''',
'''    const double FoothillEnvelope = Smooth01(FMath::Clamp(
        (BeltSignal - 0.38) / 0.38, 0.0, 1.0
    )) * MountainProvince * (1.0 - OutMountainMask * 0.52);
    const double IndependentHills = HillProvince * Smooth01(FMath::Clamp(
        (0.40 * UplandRidges
            + 0.40 * (0.5 + 0.5 * Rolling)
            + 0.20 * Province2 - 0.38) / 0.44,
        0.0, 1.0
    ));''', 'hill coverage')

          cpp = one(cpp,
'''    double Height = Uplift * Relief * 0.075
        + RegionalTilt * Relief * 0.025;''',
'''    // Zero is a world datum, not the top of the terrain. Give ordinary
    // country a modest positive regional elevation while allowing true basins
    // and rifts below it. Mountains then rise above surrounding country instead
    // of the whole map reading like one high slab with valleys cut downward.
    const double RegionalElevation = Fbm(
        Warped, Scale * 2.35,
        SeedOffset + FVector2D(2273.0, 1291.0),
        4, 0.56, 2.0
    );
    double Height = Relief * 0.060
        + RegionalElevation * Relief * 0.080
        + Uplift * Relief * 0.075
        + RegionalTilt * Relief * 0.025;''', 'elevation datum')

          insertion_anchor = '''static void SuppressPathologicalMeanders(
    const TArray<FVector>& BasePoints,'''
          thalweg = '''// Keep the displayed/carved river on the local thalweg after smoothing.
// The drainage receiver graph remains authoritative; this step only corrects
// sub-cell spline drift so a pretty curve cannot become a contour-following
// canal shelf on the side of a hill.
static void ProjectRiverToLocalThalweg(
    const FAvenorStripData& Data,
    TArray<FVector>& Points
)
{
    if (Points.Num() < 3)
    {
        return;
    }
    for (int32 Index = 1; Index + 1 < Points.Num(); ++Index)
    {
        const FVector2D Previous(Points[Index - 1]);
        const FVector2D Current(Points[Index]);
        const FVector2D Next(Points[Index + 1]);
        const FVector2D Tangent = (Next - Previous).GetSafeNormal();
        if (Tangent.IsNearlyZero())
        {
            continue;
        }
        const FVector2D Normal = Rotate90(Tangent);
        const double LocalSlope = Data.SampleGrid(Data.Slope, Current);
        const double Steepness = Smooth01(FMath::Clamp(
            (LocalSlope - 0.025) / 0.10, 0.0, 1.0
        ));
        const double SearchRadius = Data.CellSize
            * FMath::Lerp(1.45, 0.75, Steepness);
        FVector2D Best = Current;
        double BestHeight = Data.SampleGrid(Data.Height, Current);
        double BestScore = BestHeight;
        for (int32 Step = -6; Step <= 6; ++Step)
        {
            if (Step == 0)
            {
                continue;
            }
            const double Offset = SearchRadius
                * static_cast<double>(Step) / 6.0;
            const FVector2D Candidate = Current + Normal * Offset;
            if (!Data.Bounds.IsInside(FVector(Candidate.X, Candidate.Y, 0.0)))
            {
                continue;
            }
            const double CandidateHeight = Data.SampleGrid(
                Data.Height, Candidate
            );
            // Prefer the nearest low corridor. The distance penalty prevents a
            // channel from jumping across a ridge just because another valley
            // happens to be lower one or two cells away.
            const double Score = CandidateHeight + FMath::Abs(Offset) * 0.030;
            if (Score < BestScore - 40.0)
            {
                BestScore = Score;
                BestHeight = CandidateHeight;
                Best = Candidate;
            }
        }
        const double Snap = FMath::Lerp(0.62, 0.94, Steepness);
        Points[Index].X = FMath::Lerp(Current.X, Best.X, Snap);
        Points[Index].Y = FMath::Lerp(Current.Y, Best.Y, Snap);
        Points[Index].Z = BestHeight;
    }
}

'''
          if insertion_anchor not in cpp:
              raise RuntimeError('thalweg insertion anchor missing')
          cpp = cpp.replace(insertion_anchor, thalweg + insertion_anchor, 1)

          cpp = one(cpp,
'''        Points = ResamplePolyline(
            Points,
            FMath::Clamp(FeaturePointSpacing, 100.0, Data.CellSize),
            false
        );
        for (FVector& Point : Points)
        {
            const FVector2D Position(Point);
            Point.Z = Data.SampleGrid(Data.Height, Position);
        }
        EnforceDownhill(Points);''',
'''        Points = ResamplePolyline(
            Points,
            FMath::Clamp(FeaturePointSpacing, 100.0, Data.CellSize),
            false
        );
        ProjectRiverToLocalThalweg(Data, Points);
        for (FVector& Point : Points)
        {
            const FVector2D Position(Point);
            Point.Z = Data.SampleGrid(Data.Height, Position);
        }
        EnforceDownhill(Points);''', 'post-smoothing thalweg projection')

          cpp = one(cpp,
'''        River.ValleyHalfWidth = FMath::Lerp(HeadwaterValleyWidth, MainValleyWidth, RiverAlpha);
        River.ValleyDepth = FMath::Lerp(River.Depth * 1.4, MaximumValleyDepth, RiverAlpha);
        River.ChannelSteepness = ChannelSteepness;
        const double HumidValley = Smooth01(FMath::Clamp(''',
'''        River.ValleyHalfWidth = FMath::Lerp(HeadwaterValleyWidth, MainValleyWidth, RiverAlpha);
        River.ValleyDepth = FMath::Lerp(River.Depth * 1.4, MaximumValleyDepth, RiverAlpha);
        River.ChannelSteepness = ChannelSteepness;
        // On steep terrain the evolved terrain already contains the valley.
        // The Water Body should cut the channel and immediate banks, not stamp
        // an 800 m-wide level shelf sideways across a hillside.
        const double SteepValley = Smooth01(FMath::Clamp(
            (MeanSlope - 0.025) / 0.10, 0.0, 1.0
        ));
        River.ValleyHalfWidth *= FMath::Lerp(1.0, 0.38, SteepValley);
        River.ValleyDepth *= FMath::Lerp(1.0, 0.78, SteepValley);
        const double HumidValley = Smooth01(FMath::Clamp(''', 'steep valley carve')

          # Probe controls/status in actor details.
          hdr = one(hdr,
'''    UPROPERTY(EditAnywhere, Category = "Avenor|Climate", meta = (ShowOnlyInnerProperties))
    FAvenorClimateSettings Climate;
''',
'''    UPROPERTY(EditAnywhere, Category = "Avenor|Climate", meta = (ShowOnlyInnerProperties))
    FAvenorClimateSettings Climate;

    UPROPERTY(EditAnywhere, Category = "Avenor|Climate", meta = (ToolTip = "Show the live editor-camera biome/climate inspector. The console variable avenor.BiomeProbe can also disable it globally."))
    bool bShowBiomeProbe = true;
''', 'probe toggle')
          hdr = one(hdr,
'''    UPROPERTY(VisibleAnywhere, Category = "Avenor|Status")
    FString BakedDataStatus;
''',
'''    UPROPERTY(VisibleAnywhere, Category = "Avenor|Status")
    FString BakedDataStatus;

    UPROPERTY(VisibleAnywhere, Transient, Category = "Avenor|Status", meta = (MultiLine = "true"))
    FString BiomeProbeStatus;
''', 'probe status')

          probe = one(probe,
'#include "Engine/Texture2D.h"\n',
'#include "Engine/Texture2D.h"\n#include "Engine/Engine.h"\n',
'engine include')
          probe = one(probe,
'''        if (!Generator)
        {
            return true;
        }

        UAvenorTerrainData* Data''',
'''        if (!Generator || !Generator->bShowBiomeProbe)
        {
            return true;
        }

        UAvenorTerrainData* Data''', 'probe actor toggle')
          probe = one(probe,
'''        DrawDebugString(
            World,
            TextLocation,
            Status,
            nullptr,
            FColor::White,
            FMath::Max(0.10f, CVarUpdateSeconds.GetValueOnGameThread() * 1.35f),
            true,
            1.05f
        );

        return true;''',
'''        DrawDebugString(
            World,
            TextLocation,
            Status,
            nullptr,
            FColor::White,
            FMath::Max(0.10f, CVarUpdateSeconds.GetValueOnGameThread() * 1.35f),
            true,
            1.05f
        );
        Generator->BiomeProbeStatus = Status;
        if (GEngine)
        {
            GEngine->AddOnScreenDebugMessage(
                0x0A7E0B10,
                FMath::Max(0.25f, CVarUpdateSeconds.GetValueOnGameThread() * 1.8f),
                FColor::White,
                Status,
                false
            );
        }

        return true;''', 'screen probe output')

          cpp_path.write_text(cpp, encoding='utf-8')
          hdr_path.write_text(hdr, encoding='utf-8')
          probe_path.write_text(probe, encoding='utf-8')
          PY

          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add AvenorClient/Source/AvenorEditor/AvenorStripTerrainGenerator.cpp \
                  AvenorClient/Source/AvenorEditor/AvenorStripTerrainGenerator.h \
                  AvenorClient/Source/AvenorEditor/AvenorBiomeCameraProbe.cpp
          git commit -m "Keep rivers on thalwegs and diversify terrain relief"
          git push
